import { UpgradeDirective } from './upgrade.directive';

describe('UpgradeDirective', () => {
  it('should create an instance', () => {
    const directive = new UpgradeDirective();
    expect(directive).toBeTruthy();
  });
});
